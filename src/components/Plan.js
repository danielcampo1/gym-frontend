import React from 'react'

const Plan = (props) => {
    console.log(props);

    // let plan = props.workout[props.match.params.id - 1]

    // console.log(plan);
    return(
        <li>
            plan
        </li>
    )
}


export default Plan